<!doctype html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Workmatch</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            background-color: #f4f7fc;
            color: #333;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 30px;
            background: linear-gradient(90deg, #007bff, #0056b3);
            color: white;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            position: fixed;
            z-index: 1000;
            top: 0;
            left: 0;
            width: 100%;
        }

        header a:first-child {
            font-size: 1.8rem;
            font-weight: bold;
            text-decoration: none;
            color: white;
        }

        header div:last-child a {
            margin-left: 20px;
            text-decoration: none;
            font-size: 1rem;
            color: white;
            transition: color 0.3s, transform 0.3s;
        }

        header div:last-child a:hover {
            color: #ffdd57;
            transform: scale(1.1);
        }

        header div:last-child a:first-child {
            margin-left: 0;
        }

        .navbar {
            margin-top: 60px;
            background-color: #ffffff;
            padding: 10px 20px;
            border-bottom: 1px solid #ddd;
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 20px;
        }

        .navbar .menu {
            display: flex;
            gap: 15px;
        }

        .navbar a {
            color: #007bff;
            text-decoration: none;
            font-size: 1rem;
            transition: color 0.3s;
        }

        .navbar a:hover {
            color: #0056b3;
        }

        .search-bar {
            display: flex;
            align-items: center;
            gap: 10px;
            flex: 1;
        }

        .search-bar input {
            flex: 1;
            padding: 10px;
            font-size: 1rem;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        .content {
            flex: 6;
            padding: 20px;
        }

        .search-bar button {
            padding: 10px 20px;
            font-size: 1rem;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .search-bar button:hover {
            background-color: #0056b3;
        }

        .main-container {
            display: flex;
        }

        .carousel-item {
            position: relative;
            text-align: center;
            padding: 50px 20px;
            background-image: url('https://echopath.com/wp-content/uploads/2018/11/working-wirelessly-on-a-laptop-header-image-800-optimized.jpg');
            background-size: cover;
            background-position: center;
            color: white;
            border-radius: 10px;
        }

        .carousel-item::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5); /* Màu overlay với độ mờ */
            border-radius: 10px;
            z-index: 1; /* Đảm bảo overlay nằm trên ảnh nền */
        }

        .carousel-item > * {
            position: relative;
            z-index: 2; /* Nội dung nằm trên overlay */
        }




        .carousel-item h1 {
            font-size: 3rem;
            margin: 0;
        }

        .carousel-item p {
            font-size: 1.2rem;
            margin: 20px 0;
        }

         body {
             margin: 0;
             font-family: 'Arial', sans-serif;
             background-color: #f4f7fc;
             color: #333;
         }

        header {
            background: linear-gradient(to right, #f8b500, #f76d57); /* Gradient từ màu vàng ấm sang cam nhẹ */
            color: white;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 20px;
            font-size: 1.2rem;
        }


        header div {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        header a {
            color: white;
            text-decoration: none;
            font-size: 1rem;
            transition: color 0.3s;
        }

        header a:hover {
            color: #ff8c42;
        }

        .navbar {
            background-color: #ffffff;
            padding: 10px 20px;
            border-bottom: 1px solid #ddd;
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 20px;
        }

        .navbar .menu {
            display: flex;
            gap: 15px;
        }

        .navbar a {
            color: #f76d57;
            text-decoration: none;
            font-size: 1rem;
            transition: color 0.3s;
        }

        .navbar a:hover {
            color: #6c757d;
        }

        .search-bar {
            display: flex;
            align-items: center;
            gap: 10px;
            flex: 1;
        }

        .search-bar input {
            flex: 1;
            padding: 10px;
            font-size: 1rem;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        .search-bar button {
            padding: 10px 20px;
            font-size: 1rem;
            background-color: #f8b500;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.3s;
        }

        .search-bar button:hover {
            background-color: #f76d57;
            transform: scale(1.05);
        }


        .sub-navbar {
            display: flex;
            flex: 1;
            flex-direction: column;
            background-color: #f8f9fa;
            padding: 20px;
            border-right: 1px solid #ddd;
            gap: 25px;
        }

        .sub-navbar .menu-item {
            font-weight: bold;
            color: #495057;
        }

        .sub-navbar .dropdown, .sub-navbar .checkbox-group {
            padding-left: 10px;
        }

        .sub-navbar select, .sub-navbar input[type="checkbox"] {
            margin-right: 10px;
        }

        .sub-navbar select {
            width: 100%;
            padding: 8px;
            border-radius: 5px;
            border: 1px solid #ddd;
            font-size: 1rem;
        }

        .sub-navbar .checkbox-group label {
            margin-right: 15px;
            font-size: 0.9rem;
        }

        .sub-navbar .checkbox-group input {
            margin-right: 5px;
        }

        .btn {
            display: inline-block;
            padding: 15px 30px;
            font-size: 1rem;
            color: white;
            background-color: #f8b500;
            border-radius: 25px;
            text-decoration: none;
            margin-top: 20px;
            transition: background-color 0.3s, transform 0.3s;
            opacity: 0.85;
        }

        .btn:hover {
            background-color: #f76d57;
            transform: translateY(-5px);
        }


        .job-listings {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
        }

        .job-card {
            flex: 1 1 calc(33.333% - 20px);
            background: white;
            border: 1px solid #ddd;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .job-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 10px rgba(0, 0, 0, 0.2);
        }

        .job-card h3 {
            margin: 0 0 10px;
            color: #004085 ;
        }

        .job-card p {
            margin: 0 0 10px;
            font-size: 0.9rem;
            color: #666; /* Xám nhạt hơn, dễ đọc */
        }

        .job-card .apply-btn {
            display: inline-block;
            margin-top: 10px;
            padding: 10px 20px;
            font-size: 0.9rem;
            color: white;
            background-color: #28a745; /* Màu xanh lá nhẹ, tạo sự khác biệt với header */
            border-radius: 20px;
            text-decoration: none;
            transition: background-color 0.3s;
        }

        .job-card .apply-btn:hover {
            background-color: #218838; /* Màu xanh lá đậm hơn khi hover */
        }


        footer {
            text-align: center;
            padding: 20px 0;
            background-color: #333;
            color: white;
            margin-top: 40px;
        }
    </style>
</head>

<body>
<header>
    <a href="/">Workmatch</a>
    <div>
        <a href="/login">Login</a>
        <a href="/register/candidate">Register</a>
    </div>
</header>

<nav class="navbar">
    <div class="menu">
        <a href="#companies">Công ty</a>
        <a href="#jobs">Công việc</a>
        <a href="#cv">Hồ sơ</a>
    </div>
    <div class="search-bar">
        <input type="text" placeholder="Tìm kiêm công việc, công ty ...">
        <button type="button">Search</button>
    </div>
</nav>



<div class="main-container">

    <!-- Sub navbar -->

    <div class="sub-navbar">
        <div class="menu-item">Thành phố</div>
        <div class="dropdown">
            <select name="city">
                <!-- Thêm lựa chọn "Tất cả thành phố" đầu tiên -->
                <option value="" th:text="'Tất cả'"></option>

                <!-- Lặp qua danh sách thành phố từ model -->
                <th:block th:each="city : ${cities}">
                    <option th:value="${city.id}" th:text="${city.name}"></option>
                </th:block>
            </select>
        </div>



        <!-- Skills -->
        <div class="menu-item">Skills</div>
        <div class="checkbox-group">
            <label><input type="checkbox" name="skills" value="JavaScript"> JavaScript</label>
            <label><input type="checkbox" name="skills" value="Python"> Python</label>
            <label><input type="checkbox" name="skills" value="React"> React</label>
            <label><input type="checkbox" name="skills" value="Node.js"> Node.js</label>
        </div>
    </div>

    <!-- Main content -->

    <div class="content">
        <section class="hero">
            <div id="heroCarousel" class="carousel slide" data-bs-ride="carousel">
                <div class="carousel-inner">
                    <div class="carousel-item active">
                        <h1>Tìm Kiếm Công Việc Mơ Ước Của Bạn</h1>
                        <p>Duyệt qua hàng nghìn cơ hội việc làm và ứng tuyển ngay hôm nay!</p>
                        <a href="#job-listings" class="btn">Khám Phá Các Công Việc</a>
                    </div>
                    <div class="carousel-item">
                        <h1>Gia Nhập Các Công Ty Hàng Đầu</h1>
                        <p>Làm việc với các nhà lãnh đạo ngành và phát triển sự nghiệp của bạn.</p>
                        <a href="#job-listings" class="btn">Khám Phá Các Công Việc</a>
                    </div>
                    <div class="carousel-item">
                        <h1>Tương Lai Của Bạn Đang Chờ</h1>
                        <p>Tìm công việc phù hợp với mục tiêu và khát vọng của bạn.</p>
                        <a href="#job-listings" class="btn">Khám Phá Các Công Việc</a>
                    </div>
                </div>
                <button class="carousel-control-prev" type="button" data-bs-target="#heroCarousel" data-bs-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Trước</span>
                </button>
                <button class="carousel-control-next" type="button" data-bs-target="#heroCarousel" data-bs-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Tiếp Theo</span>
                </button>
            </div>
        </section>

        <br>

        <section id="job-listings">
            <h2 style="text-align: center; margin-bottom: 20px;">Job Openings</h2>
            <div class="job-listings">
                <div class="job-card">
                    <h3>Software Engineer</h3>
                    <p>Location: San Francisco, CA</p>
                    <p>Experience: 2-4 years</p>
                    <a href="#" class="apply-btn">Apply Now</a>
                </div>
                <div class="job-card">
                    <h3>Product Manager</h3>
                    <p>Location: New York, NY</p>
                    <p>Experience: 3-5 years</p>
                    <a href="#" class="apply-btn">Apply Now</a>
                </div>
                <div class="job-card">
                    <h3>UI/UX Designer</h3>
                    <p>Location: Remote</p>
                    <p>Experience: 1-3 years</p>
                    <a href="#" class="apply-btn">Apply Now</a>
                </div>
            </div>
        </section>
    </div>
</div>

<footer>
    &copy; 2024 Workmatch - IT Job Recruitment Portal. Created by sanghynh.
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>